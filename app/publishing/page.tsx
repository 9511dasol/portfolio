import Publishings from "../../components/publishing";

export const metadata = {
  title: "Publishings",
}; // 레이아웃에서 굳이 보여줄 필요 없다.

function Publishing() {
  return <Publishings />;
}

export default Publishing;
